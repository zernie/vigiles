{
  "rules": {
    "require-spec": "error",
    "require-skill-spec": "warn",
    "freshness": ["warn", { "mode": "strict" }],
    "coverage": ["warn", { "scripts": 50, "linterRules": 5 }],
    "drift": "warn"
  }
}
