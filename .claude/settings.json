{
  "hooks": {
    "SessionStart": [
      {
        "command": "export PATH=\"/opt/rbenv/shims:$PATH\" && npm install && npm run build && pip install ruff pylint && gem install rubocop"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "npx vigiles CLAUDE.md"
      },
      {
        "matcher": "Edit|Write",
        "command": "npx prettier --check ."
      }
    ]
  }
}
