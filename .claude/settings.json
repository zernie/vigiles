{
  "hooks": {
    "SessionStart": [
      {
        "command": "export PATH=\"/opt/rbenv/versions/3.3.6/bin:$PATH\" && npm install && npm run build && pip install ruff pylint && gem install rubocop && rbenv rehash 2>/dev/null; true"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "npx vigiles CLAUDE.md"
      },
      {
        "matcher": "Edit|Write",
        "command": "npx prettier --check ."
      }
    ]
  }
}
