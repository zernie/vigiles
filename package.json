{
  "name": "agent-lint",
  "private": true,
  "bin": {
    "agent-lint": "./validate.mjs"
  },
  "scripts": {
    "test": "node --test validate.test.mjs",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  },
  "dependencies": {
    "cosmiconfig": "^9.0.1"
  }
}
