# Playwright / Vitest browser-mode artifacts
test-results/
playwright-report/
blob-report/
.last-run.json

# Vitest browser-mode attachments/screenshots
.vitest-attachments/
**/__screenshots__/

# Generated static pages (built by prebuild → scripts/gen-check-pages.ts).
# Leading slash anchors to the site root so src/checks/ (the SOURCE) stays tracked.
/checks/
/comparison/
