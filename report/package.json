{
  "name": "@vigiles/report",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The vigiles audit report UI — Vite + React + shadcn, built to a single self-contained HTML file the CLI fills with an AuditReport. Kept a separate package so its frontend toolchain never enters the published CLI's runtime deps; the only shipped artifact is the built template (dist/audit-report.template.html).",
  "scripts": {
    "build": "vite build",
    "dev": "vite"
  },
  "dependencies": {
    "@vigiles/report-view": "*",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.469.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwind-merge": "^2.6.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.1.1",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^8.3.0",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
